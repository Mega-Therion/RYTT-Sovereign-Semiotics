__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Lean/Lake build artifacts and fetched dependencies (Mathlib et al.) — regenerate with
# `lake build` after `lake exe cache get` or by pointing at a sibling checkout; never commit.
proofs/.lake/
proofs/lake-manifest.json
benchmarks/results/

# Local build and interpreter artifacts
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
